const {
    WAConnection
} = require('../lib/modified');
const {
    WAConnection: _WAConnection
} = require('@adiwajshing/baileys')

const eventHandler = async (client = new WAConnection()) => {
    
}

module.exports = eventHandler